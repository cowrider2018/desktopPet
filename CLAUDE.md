# Claude Code 專案開發規範

## 關鍵指令
- 啟動開發：`npm run dev`
- 語法檢查：`npm run lint`
- 執行所有測試：`npm run test`
- 執行單一測試：`npm run test -- <file-path>` （若框架不同，請先確認 package.json 測試指令）

## 代碼風格
- 嚴格使用 TypeScript，嚴禁使用 `any`（優先推導型別）。
- 除路由（Route）檔案外，其餘一律使用「具名匯出 (Named Exports)」。
- UI 組件一律放在 `src/components/ui/`，且單一檔案行數不可超過 200 行。

## AI 行為守則
1. **最小化改動**：只觸碰與任務直接相關的程式碼，嚴禁順便重構不相干的檔案。
2. **不盲目猜測**：若遇程式碼邏輯矛盾或上下文模糊，必須立即停止並向使用者確認，嚴禁自行通融。
3. **完成度驗證**：每次修改完畢，必須主動執行 `npm run lint` 與相關測試，確保環境無誤後才回報。

## Git 操作規範
### 核心原則
1. **單一任務提交**：每完成一個獨立微小變更，必須立即 commit，嚴禁累積大量改動後一次提交。
2. **嚴禁強制推送**：禁止使用 `git push --force` 或 `-f`。若遇衝突，必須立即停止，並逐一詢問使用者以解決每個衝突點。

### 工作流程
收到任務結束提示或執行 `/commit` 時，遵循以下步驟：
1. **Commit 規範**：
   * 分析 `git diff`，採用 Conventional Commits 規範（如 `feat:`, `fix:`, `docs:`）。
   * 訊息須簡潔且精確描述實質變更。
2. **Push 規範**：
   * **預設**：僅執行 commit，禁止自動 push。
   * **觸發條件**：僅當指示包含關鍵字「**並 push**」時，方可執行 `git push origin <當前分支>`。

## 需求分析與執行協議
### 處理流程
收到複雜需求時，必須嚴格依序執行以下兩階段，嚴禁未經審核直接修改程式碼：

### 第一階段：分析與提案（等待審核）
1. **需求提取**：解析核心意圖、邊界條件與技術痛點。
2. **多軌方案**：提出可行方案（1~3 個），並以固定格式表格對比優劣：


   | 方案名稱 | 實作難易度 | 優點 (Pros) | 缺點 (Cons) | 推薦指數(1~5) |
   | :--- | :--- | :--- | :--- | :--- |

3. **流程視覺化**：繪製「Text-based/ASCII」的系統流程圖，展現資料流或邏輯走向。
4. **卡點審核**：明確推薦最優解，並提示：「**請審核以上方案與流程圖，確認後將開始執行。**」此時必須停止動作，靜待使用者指示。

### 第二階段：計畫與執行
1. **任務拆解**：使用者選定方案後，列出具體步驟清單（Todo List）。
2. **小步執行**：依序執行，每完成一個獨立子任務，必須嚴格遵循《Git 操作規範》進行單一任務 commit。
